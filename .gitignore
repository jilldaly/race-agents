.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
.env

# personal notes (kept local, not part of the project)
GIT_SETUP.md

# bronze raw data is committed (4.3M — small enough to keep the repo + CI
# self-contained so the golden eval can run); the object-store bucket remains the
# canonical source once hosted. .DS_Store is ignored globally above.

# silver/gold are per-agent and always regenerable
**/silver/
**/reports/
**/output/
