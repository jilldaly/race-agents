.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
.env

# personal notes (kept local, not part of the project)
GIT_SETUP.md

# local-only build diary (private blog raw material; kept out of the repo)
JOURNAL.md

# bronze: the currently-committed Cork PDFs stay (CI's golden eval builds silver
# from them). Don't auto-add NEW raw data to the repo — keep new bronze out of git.
data/bronze/cork/

# silver/gold are per-agent and always regenerable
**/silver/
**/reports/
**/output/
deploy/hf-space/space/

# sample report lives in the cork-city-marathon-analysis repo; not duplicated here
sample_report/
