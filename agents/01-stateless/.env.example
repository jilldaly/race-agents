# Agent 01 — control plane (Gemini free tier, OpenAI-compatible endpoint).
# Copy to .env (gitignored) and fill in your free-tier key.
GEMINI_API_KEY=

# Defaults (override only if needed):
LLM_MODEL=gemini-2.0-flash
LLM_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/
LLM_TEMPERATURE=0
AGENT_MAX_STEPS=6
