# Bronze store backend: local (default) | s3 | gcs
BRONZE_BACKEND=local
BRONZE_ROOT=data/bronze

# LLM transport (OpenAI-compatible). Default = local Ollama, $0.
OPENAI_BASE_URL=http://localhost:11434/v1
OPENAI_API_KEY=ollama
MODEL=llama3.1:8b
